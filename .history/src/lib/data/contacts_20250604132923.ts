import { m } from "$lib/paraglide/messages";

const contacts = {
    title: m["contact.page.title"](),
    note: m["contact.page.note"](),
    email: "Garbage@ProWebber.ca"
}

const form = {
    title: m["contact.page.form.title"](),
    email: m["contact.page.form.email"](),
    message: m["contact.page.form.message"](),
    send: m["contact.page.form.send"](),
    reset: m["contact.page.form.reset"]()
}