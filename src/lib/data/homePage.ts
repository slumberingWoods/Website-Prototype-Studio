import { m } from "$lib/paraglide/messages";

export const hero = {
    title: "Website Prototype",
    motto: m["home.page.motto"]()
}

export const newsletter = {
    title: m["home.page.newsletter"](),
    subscribe: m["home.page.subscribe"]()
}